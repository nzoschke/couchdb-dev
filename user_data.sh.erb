#!/bin/bash
# script is run on first boot; EC2 has 16k limit
apt-get update && apt-get install --yes --force-yes openssh-server git-core ruby rubygems
cd /
git init
git remote add github-ro git://github.com/nzoschke/<%= name %>.git
git pull github-ro master
./boot.sh # pass control non-userdata script
